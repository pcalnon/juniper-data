---
#####################################################################################################################################################################################################
# Project:       Juniper
# Sub-Project:   JuniperData
# Application:   juniper_data
# File Name:     lockfile-update.yml
# Author:        Paul Calnon
# Version:       0.1.0
#
# Date Created:  2026-03-02
# Last Modified: 2026-03-02
#
# License:       MIT License
# Copyright:     Copyright (c) 2024-2026 Paul Calnon
#
# Description:
#    Automatically updates requirements.lock when Dependabot pushes a dependency
#    update to a dependabot/pip/** branch. Runs uv pip compile to regenerate
#    the lockfile and commits the result so Docker builds stay in sync.
#
#    Uses CROSS_REPO_DISPATCH_TOKEN (not GITHUB_TOKEN) so the push re-triggers CI.
#
# References:
#    - Step 7.5.2: Dependency Update Workflow (POLYREPO_MIGRATION_PLAN.md)
#####################################################################################################################################################################################################

name: Update Lockfile (Dependabot)

on:
  push:
    branches:
      - dependabot/pip/**
  pull_request:
    paths:
      - pyproject.toml

permissions:
  contents: write

jobs:
  update-lockfile:
    name: Update requirements.lock
    runs-on: ubuntu-latest
    # Run for:
    #   - any push to a dependabot/pip/** branch (the original trigger), AND
    #   - pull_request events from this repo's own branches (covers
    #     manual pyproject.toml edits — e.g. min-version bumps that
    #     leave main red after merge per the 2026-05-11 pathspec >=1.1.1
    #     incident that vanished requirements.lock entirely). Skip PRs
    #     from forks since they cannot push back with our token.
    # Replicates juniper-canopy's PR #250 hardening (memory note
    # "canopy lockfile-update.yml auto-regenerates on pull_request").
    if: |
      (github.event_name == 'push' && github.actor == 'dependabot[bot]') ||
      (github.event_name == 'pull_request' && github.event.pull_request.head.repo.full_name == github.repository)

    steps:
      - name: Checkout Code
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1
        with:
          token: ${{ secrets.CROSS_REPO_DISPATCH_TOKEN }}
          # For pull_request events, check out the PR head branch (not the
          # merge ref) so the regen commit can be pushed back. ${{ github.ref }}
          # remains the right value for push events on dependabot/pip/**.
          ref: ${{ github.head_ref || github.ref }}

      - name: Set up Python 3.14
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97  # v7.0.0
        with:
          python-version: "3.14"

      - name: Install uv
        run: pip install uv==0.11.8

      - name: Regenerate requirements.lock
        run: |
          echo "╔════════════════════════════════════════════════════════════╗"
          echo "║       JuniperData - Regenerate Lockfile                    ║"
          echo "╚════════════════════════════════════════════════════════════╝"

          # Write directly to requirements.lock so the autogenerated header
          # records "-o requirements.lock" rather than a temp path. --upgrade
          # is required for Dependabot bumps: when a transitive pin in the
          # current lockfile blocks the bumped direct dep, --constraint mode
          # without --upgrade would refuse to move it. Direct deps that
          # Dependabot updates land via pyproject.toml diffs.
          uv pip compile pyproject.toml \
            --extra api \
            --extra observability \
            --extra mnist \
            --upgrade \
            -o requirements.lock

      - name: Commit updated lockfile
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add requirements.lock
          if git diff --cached --quiet; then
            echo "::notice::Lockfile already up to date — no commit needed"
          else
            git commit -m "[dependabot skip] Update requirements.lock"
            git push
          fi
